apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-agent.fullname" . }}-backend
  labels:
    app: {{ include "dclaw-agent.name" . }}
    component: backend
spec:
  type: {{ .Values.service.backend.type }}
  ports:
    - port: {{ .Values.service.backend.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-agent.name" . }}
    component: backend

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-agent.fullname" . }}-frontend
  labels:
    app: {{ include "dclaw-agent.name" . }}
    component: frontend
spec:
  type: {{ .Values.service.frontend.type }}
  ports:
    - port: {{ .Values.service.frontend.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "dclaw-agent.name" . }}
    component: frontend
